# auto-generated by grc.converter

id: learning_header_reader
label: Packet header reader
category: '[learning]'

parameters:
-   id: header_byte_length
    label: Header byte length
    dtype: int
    default: '2'
-   id: preamble_byte_length
    label: Preamble byte length
    dtype: int
    default: '5'
-   id: tag_name
    label: Tag name
    dtype: string
    default: '"header_start"'
-   id: new_tag_name
    label: New tag name
    dtype: string
    default: '"step"'
-   id: threshold
    label: Outlier detection threshold
    dtype: int
    default: '10'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import learning
    make: learning.header_reader(${header_byte_length}, ${preamble_byte_length}, ${tag_name},
        ${new_tag_name}, ${threshold})

file_format: 1
