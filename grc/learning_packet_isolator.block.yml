# auto-generated by grc.converter

id: learning_packet_isolator
label: Packet isolator
category: '[learning]'

parameters:
-   id: pack_len
    label: Packet length
    dtype: int
    default: '100'
-   id: pay_len
    label: payload length
    dtype: int
    default: '100'
-   id: lookup_len
    label: lookup window
    dtype: int
    default: '30'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: from gnuradio import learning
    make: learning.packet_isolator(${pack_len}, ${pay_len}, ${lookup_len})

file_format: 1
