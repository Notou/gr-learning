# auto-generated by grc.converter

id: learning_data_switch
label: Header based switch
category: '[learning]'

parameters:
-   id: tx_amount
    label: Tx amount
    dtype: int
    default: '2'
    hide: part
-   id: header_size
    label: Header size
    dtype: int
    default: '380'
-   id: payload_size
    label: Payload size
    dtype: int
    default: '600'
-   id: to_server
    label: Send to server
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
-   id: address
    label: Server IP
    dtype: string
    default: 127.0.0.1
    hide: ${ (none if to_server else all) }
-   id: port
    label: Server port
    dtype: int
    default: '3581'
    hide: ${ (none if to_server else all) }

inputs:
-   label: id
    domain: stream
    dtype: byte
-   label: frame
    domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ tx_amount }

templates:
    imports: from gnuradio import learning
    make: learning.data_switch(${tx_amount}, ${header_size}, ${payload_size}, ${to_server},
        ${address}, ${port})

file_format: 1
