# auto-generated by grc.converter

id: learning_error_estimator
label: Error estimator
category: '[learning]'

parameters:
-   id: tag_name
    label: Tag Name
    dtype: string
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, byte]
    option_attributes:
        fcn: ['"cc"', '"ff"', '"ii"', '"bb"']
    hide: part
-   id: interval
    label: Frame size
    dtype: int
    default: '100'
-   id: packet_loss_penalty
    label: Packet loss penalty
    dtype: float
    default: '10'

inputs:
-   label: reference
    domain: stream
    dtype: ${ type }
    multiplicity: '1'
-   label: signal
    domain: stream
    dtype: ${ type }
    multiplicity: '1'

outputs:
-   domain: message
    id: error
    optional: true

templates:
    imports: import learning
    make: learning.error_estimator(${tag_name}, ${interval}, ${type.fcn}, ${packet_loss_penalty})

file_format: 1
