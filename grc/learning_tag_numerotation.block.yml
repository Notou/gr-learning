# auto-generated by grc.converter

id: learning_tag_numerotation
label: Tag numerotation
category: '[learning]'

parameters:
-   id: tag_name
    label: Tag Name
    dtype: string
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, byte]
    option_attributes:
        fcn: ['"cc"', '"ff"', '"ii"', '"bb"']
    hide: part
-   id: interval
    label: Tag interval
    dtype: int
    default: '100'
-   id: modulo
    label: Numbering modulo
    dtype: int
    default: '4096'

inputs:
-   domain: stream
    dtype: ${ type }

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import learning
    make: learning.tag_numerotation(${tag_name}, ${interval}, ${modulo}, ${type.fcn})

file_format: 1
