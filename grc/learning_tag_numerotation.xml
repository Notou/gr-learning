<?xml version="1.0"?>
<block>
  <name>Tag numerotation</name>
  <key>learning_tag_numerotation</key>
  <category>[learning]</category>
  <import>import learning</import>
  <make>learning.tag_numerotation($tag_name, $interval, $(type.fcn))</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Tag Name</name>
    <key>tag_name</key>
    <type>string</type>
  </param>

  <param>
  		<name>IO Type</name>
  		<key>type</key>
  		<type>enum</type>
  		<option>
  			<name>Complex</name>
  			<key>complex</key>
  			<opt>fcn:"cc"</opt>
  		</option>
  		<option>
  			<name>Float</name>
  			<key>float</key>
  			<opt>fcn:"ff"</opt>
  		</option>
  		<option>
  			<name>Int</name>
  			<key>int</key>
  			<opt>fcn:"ii"</opt>
  		</option>
  		<option>
  			<name>Byte</name>
  			<key>byte</key>
  			<opt>fcn:"bb"</opt>
  		</option>
  	</param>

  <param>
    <name>Tag interval</name>
    <key>interval</key>
    <value>100</value>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>$type</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>$type</type>
  </source>
</block>
