# auto-generated by grc.converter

id: learning_packet_isolator_c
label: Packet isolator c
category: '[learning]'

parameters:
-   id: payload_length
    label: Payload_length
    dtype: int
    default: '0'
-   id: preamble_length
    label: Preamble_length
    dtype: int
    default: ' 80'
-   id: lookup_window
    label: Lookup_window
    dtype: int
    default: ' 30'
-   id: tag_name
    label: Tag_name
    dtype: string
    default: '"corr_est"'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: from gnuradio import learning
    make: learning.packet_isolator_c(${payload_length}, ${preamble_length}, ${lookup_window},
        ${tag_name})

file_format: 1
