# auto-generated by grc.converter

id: learning_udp_trigger
label: UDP Trigger
category: '[learning]'

parameters:
-   id: tx_number
    label: Tx Number
    dtype: int
-   id: ip
    label: IP address
    dtype: string
    default: '''127.0.0.1'''
-   id: port
    label: Port Number
    dtype: int

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import learning
    make: learning.udp_trigger(${tx_number},${ip},${port})

file_format: 1
