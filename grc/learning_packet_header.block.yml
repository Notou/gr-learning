# auto-generated by grc.converter

id: learning_packet_header
label: Packet number inserter
category: '[learning]'

parameters:
-   id: header_byte_length
    label: Header byte length
    dtype: int
    default: '2'
-   id: preamble_byte_length
    label: Preamble byte length
    dtype: int
    default: '5'
-   id: tag_name
    label: Tag name
    dtype: string
    default: '"step"'
-   id: payload_byte_length
    label: Payload byte length
    dtype: int
    default: '10'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: from gnuradio import learning
    make: learning.packet_header(${header_byte_length}, ${preamble_byte_length}, ${tag_name},
        ${payload_byte_length})

file_format: 1
